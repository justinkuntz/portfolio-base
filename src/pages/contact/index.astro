---
import PageLayout from "@layouts/PageLayout.astro";
import Container from "@components/Container.astro";
import Button from "@components/Button.astro";
import { CONTACT } from "@consts";

const searchParams = Astro.url.searchParams;
const statusParam = searchParams.get("status");
const reasonParam = searchParams.get("reason");

const statusCopy = (() => {
  if (statusParam === "success") {
    return {
      tone: "success",
      text: "Thanks for reaching out! We'll reply as soon as possible.",
    };
  }
  if (statusParam === "error") {
    const text =
      reasonParam === "rate-limit"
        ? "You're sending messages a bit too quickly. Please wait a moment and try again."
        : "We couldn't send your message. Please try again in a moment.";
    return { tone: "error", text };
  }
  return null;
})();
---

<PageLayout title={CONTACT.TITLE} description={CONTACT.DESCRIPTION}>
  <Container size="lg">
    <section class="contact">
      <div class="contact__grid">
        <div class="contact__intro">
          <p class="contact__label">{CONTACT.TITLE}</p>
          <h1 class="contact__title">
            Let's build something together
          </h1>
          <p class="contact__description">
            Tell me about your project, collaboration idea, or speaking request
            and I will get back to you within two business days.
          </p>
          <ul class="contact__list">
            <li class="contact__list-item">
              <span class="contact__list-label">Availability</span>
              <span>Accepting new work for Q3–Q4.</span>
            </li>
            <li class="contact__list-item">
              <span class="contact__list-label">Response time</span>
              <span>Typically within 48 hours.</span>
            </li>
          </ul>
        </div>
        <div class="contact__form-wrap">
          {
            statusCopy && (
              <div
                class={`contact__status contact__status--${statusCopy.tone}`}
              >
                {statusCopy.text}
              </div>
            )
          }
          <form
            id="contact-form"
            action="/api/contact"
            method="post"
            class="contact__form"
            novalidate
          >
            <input type="hidden" name="startedAt" id="contact-started" />
            <label class="sr-only" for="contact-company">Company</label>
            <input
              id="contact-company"
              name="company"
              type="text"
              tabindex="-1"
              autocomplete="off"
              aria-hidden="true"
              class="contact__hidden"
            />

            <div class="contact__field">
              <label class="contact__label" for="contact-name">Name</label>
              <input
                id="contact-name"
                name="name"
                type="text"
                required
                minlength="2"
                placeholder="Olive Oyl"
                class="contact__input"
              />
            </div>

            <div class="contact__field">
              <label class="contact__label" for="contact-email">Email</label>
              <input
                id="contact-email"
                name="email"
                type="email"
                required
                placeholder="olive@oyl.studio"
                class="contact__input"
              />
            </div>

            <div class="contact__field">
              <div class="contact__field-head">
                <label class="contact__label" for="contact-message">Message</label>
                <span class="contact__hint">Minimum 20 characters</span>
              </div>
              <textarea
                id="contact-message"
                name="message"
                required
                minlength="20"
                rows="6"
                placeholder="Share what you're working on, timelines, and anything else that's helpful."
                class="contact__input contact__textarea"
              ></textarea>
            </div>

            <div class="contact__submit-wrap">
              <p class="contact__hint">
                By submitting you agree to receive a reply at the email address
                you provided.
              </p>
              <Button type="submit" variant="primary" size="lg" class="contact__submit">
                <span class="inline" data-submit-text>Send message</span>
              </Button>
              <p
                id="contact-status"
                role="status"
                aria-live="polite"
                class="contact__status-msg"
                hidden
              >
              </p>
            </div>
          </form>
        </div>
      </div>
    </section>
  </Container>
</PageLayout>

<style>
  .contact {
    padding-block: var(--space-24);
  }

  .contact__grid {
    display: grid;
    gap: var(--space-12);
  }

  @media (min-width: 1024px) {
    .contact__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-20);
    }
  }

  .contact__intro {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .contact__label {
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: rgb(from var(--color-surface-950) r g b / 0.6);
  }

  .contact__title {
    font-size: var(--text-4xl);
    font-weight: var(--font-semibold);
    color: var(--color-surface-950);
    letter-spacing: -0.025em;
    margin: 0;
  }

  @media (min-width: 768px) {
    .contact__title {
      font-size: var(--text-5xl);
    }
  }

  .contact__description {
    font-size: var(--text-lg);
    color: rgb(from var(--color-surface-950) r g b / 0.8);
    margin: 0;
  }

  .contact__list {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    color: rgb(from var(--color-surface-950) r g b / 0.7);
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .contact__list-item {
    display: flex;
    gap: var(--space-3);
  }

  .contact__list-label {
    font-weight: var(--font-medium);
    color: var(--color-surface-950);
  }

  .contact__form-wrap {
    background-color: var(--color-surface-50);
    border-radius: var(--radius-md);
    border: 1px solid var(--color-surface-300);
    box-shadow: var(--shadow-sm);
    padding: var(--space-6);
  }

  @media (min-width: 640px) {
    .contact__form-wrap {
      padding: var(--space-8);
    }
  }

  .contact__status {
    margin-bottom: var(--space-6);
    border-radius: var(--radius-md);
    border: 1px solid;
    padding: var(--space-3) var(--space-4);
    font-size: var(--text-sm);
  }

  .contact__status--success {
    border-color: rgb(16 185 129 / 0.6);
    background-color: rgb(16 185 129 / 0.1);
    color: var(--color-surface-900);
  }

  html.dark .contact__status--success {
    color: rgb(167 243 208);
  }

  .contact__status--error {
    border-color: rgb(244 63 94 / 0.6);
    background-color: rgb(244 63 94 / 0.1);
    color: var(--color-surface-900);
  }

  html.dark .contact__status--error {
    color: rgb(253 164 175);
  }

  .contact__form {
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
  }

  .contact__field {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .contact__field-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: var(--text-sm);
  }

  .contact__hint {
    font-size: var(--text-sm);
    color: rgb(from var(--color-surface-950) r g b / 0.6);
    margin: 0;
  }

  .contact__input {
    width: 100%;
    padding: var(--space-3) var(--space-4);
    border-radius: var(--radius-md);
    border: 1px solid var(--color-surface-300);
    background-color: var(--color-surface-50);
    color: var(--color-surface-950);
    font-family: var(--font-sans);
    font-size: var(--text-base);
    outline: none;
    transition: border-color var(--transition-base), box-shadow var(--transition-base);
  }

  .contact__input:focus-visible {
    border-color: rgb(from var(--color-surface-950) r g b / 0.7);
    box-shadow: 0 0 0 2px rgb(from var(--color-surface-950) r g b / 0.2);
  }

  .contact__textarea {
    resize: vertical;
    min-height: 8rem;
  }

  .contact__hidden {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }

  .contact__submit-wrap {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .contact__submit {
    width: 100%;
  }

  .contact__submit:disabled {
    cursor: not-allowed;
    opacity: 0.6;
  }

  .contact__status-msg {
    font-size: var(--text-sm);
    margin: 0;
  }

  #contact-status[data-variant="success"] {
    color: rgb(16 185 129 / 0.95);
  }

  #contact-status[data-variant="error"] {
    color: rgb(244 63 94 / 0.95);
  }
</style>

<script>
  const form = document.getElementById("contact-form") as HTMLFormElement | null;
  const status = document.getElementById("contact-status");
  const submitButton = form?.querySelector("button[type=submit]") as HTMLButtonElement | null | undefined;
  const startedAtInput = document.getElementById("contact-started") as HTMLInputElement | null;

  function setTimestamp() {
    if (startedAtInput) {
      startedAtInput.value = Date.now().toString();
    }
  }

  function setStatus(message: string, variant: string) {
    if (!status) return;
    status.textContent = message;
    status.dataset.variant = variant;
    status.hidden = false;
  }

  function clearStatus() {
    if (!status) return;
    status.textContent = "";
    status.removeAttribute("data-variant");
    status.hidden = true;
  }

  async function handleSubmit(event: Event) {
    event.preventDefault();
    if (!form || !submitButton) return;

    clearStatus();

    const formData = new FormData(form);
    const message = (formData.get("message") || "").toString().trim();

    if (message.length < 20) {
      setStatus(
        "Please include at least 20 characters so I have context.",
        "error",
      );
      return;
    }

    submitButton.disabled = true;

    formData.set("startedAt", startedAtInput?.value || Date.now().toString());

    try {
      const response = await fetch(form.action, {
        method: "POST",
        headers: {
          "X-Requested-With": "contact-form",
        },
        body: formData,
      });

      const payload = await response.json().catch(() => null);

      if (!response.ok) {
        throw new Error(
          payload?.message ||
            "We couldn't send your message. Please try again.",
        );
      }

      form.reset();
      setTimestamp();
      setStatus(payload?.message || "Thanks for reaching out!", "success");
    } catch (error) {
      const fallback =
        "We couldn't send your message. Please try again in a moment.";
      setStatus(error instanceof Error ? error.message : fallback, "error");
    } finally {
      submitButton.disabled = false;
    }
  }

  function init() {
    setTimestamp();
    form?.addEventListener("submit", handleSubmit);
  }

  init();
  document.addEventListener("astro:after-swap", () => {
    clearStatus();
    setTimestamp();
  });
</script>
